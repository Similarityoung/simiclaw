// Package port defines the minimal ingest persistence and scope lookup contracts.
// It is intentionally free of API/service wiring so store-side implementations can
// depend on it without importing the ingest application package.
package port
